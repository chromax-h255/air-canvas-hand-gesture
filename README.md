# Air Canvas (Hand Gesture)

A virtual drawing canvas using MediaPipe and OpenCV that lets you draw in the air with hand gestures.  
Supports color selection and an eraser using finger movement.

## Requirements
- Python 3.8+
- OpenCV
- MediaPipe
- NumPy

## Run
python air_canvas.py
